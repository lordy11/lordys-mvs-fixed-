# Lordy's MVS Fixed

This repository contains only the clean Modular Vest System source prepared for DayZ Tools Addon Builder.

After the import finishes, pack:

- Source: `pack\ModularVestSystem`
- Destination: `pack\output\@MVS\Addons`
- Prefix: `ModularVestSystem`
- Direct-copy list: `pack\AddonBuilder_Include.txt`
- Enable Binarize and Clear temporary folder.
- Leave Project path empty/default.

Copy `pack\Mod_Files\mod.cpp` and `meta.cpp` into the finished `@MVS` folder.
